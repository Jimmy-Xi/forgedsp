# Contributing

1. Preserve `valid/ready` semantics under arbitrary backpressure.
2. Add or update an independent bit-exact model for arithmetic changes.
3. Add cocotb coverage for every RTL behavior change.
4. Add a formal property when changing buffering or control flow.
5. Report generic synthesis separately from named-device place-and-route results.

